-- NOTE, THIS FILE IS BEING USED by disabling Laptop display monitor behaviour when closing lid.
-- See notes on Laptops.conf
--
-- This file is auto-generated/overwritten at runtime by the Laptops.conf bindl
-- hooks (see UserConfigs/Laptops.lua), which echo a `monitor = ...` line into
-- the real .conf file when the lid switch fires. As a static Lua snapshot it
-- just mirrors whatever that last write currently contains.

local M = {}

-- M.monitor = "eDP-1, preferred, auto, 1" -- (disabled / not currently set)

return M